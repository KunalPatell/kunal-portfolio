# Only needed for SPLIT local dev (Next on :3000, FastAPI on :8000).
# In the single-container Hugging Face deploy the API is same-origin, so
# leave this UNSET in production.
NEXT_PUBLIC_API_URL=http://localhost:8000
